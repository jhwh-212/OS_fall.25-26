{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CPU Scheduler",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/scheduler.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build scheduler"
        }
    ]
}
